export interface SpanConfig {
    tag: string;
    defaultValue: number;
    min: number;
    max: number;
}
export interface Config {
    cellSelector: string;
    rowSelector: string;
    col: SpanConfig;
    row: SpanConfig;
}
export declare const defaultConfig: Config;
